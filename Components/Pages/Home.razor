@page "/"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@inject NavigationManager Nav

<PageTitle>kd-802x-portal</PageTitle>
<p>ダッシュボードへ移動しています…</p>

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/dashboard", replace: true);
    }
}
